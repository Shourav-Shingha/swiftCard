# swiftCard
1) What is the difference between null and undefined?
ans:- nal is something that dosenot exit. and the undefiend is that which exit but that time its not found or dosn't mention

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
ans:- map go to all the element do there work but print an array. forEach() print all single element of an array.

3) What is the difference between == and ===?
ans:- to match elements == converts elements, one is number or the other string then == converts one of then to other primitive data then match. but === dosenot converts them. its ensure number= number, string=string.

4) What is the significance of async/await in fetching API data?
ams:- in my openion .then is deficult to write. sometime forgen abour write dot or after [.then((res)=>res.json());] if someone write this(;) and dont notice then then the code never work. so its safe from .then

5) Explain the concept of Scope in JavaScript (Global, Function, Block).
ans:- scoop defind thats js creat some barrier in every step. lets thing about a house with4 side barrier. then no one can see what inside the barrier. it's true if someone try to see what inside. after do some work they get just some low level information.  but the inside people can see, heard, and call all of them which are the outside.



note{ that is mention that i have to anwser all the question in bangla. i used ubuntu i downlode avro in my laptop but it's dosen't work properly and i have not the habit to write bangla in laptop. so i anwserd all in english. Really sorry for that.}